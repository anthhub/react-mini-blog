export default function reducer(state: any, action: any) {
  switch (action.type) {
    case 'ADD_TODO':
      return state
    case 'COMPLETE':
      return state
    default:
      return state
  }
}
