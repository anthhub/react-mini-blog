// import styled from 'styled-components';

// export const Wrapper = styled.div`

// `

import styled from 'styled-components'

export const Wrapper = styled.div`
	display: flex;
	flex-direction: row;
	justify-content: space-between;
	/* 交叉轴对齐方式默认 stretch 会纵向拉伸 AppDownload 组件（未设置高度） */
	align-items: flex-start;
	width: 960px;
	margin: 76px auto 0;
	/* background: skyblue; */
`
