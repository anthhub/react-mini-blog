// import styled from 'styled-components';

// export const Wrapper = styled.div`

// `

import styled from 'styled-components';

export const Wrapper = styled.div`
	display: flex;
	justify-content: space-between;
	width: 960px;
	margin: 82px auto 0;

	.left {
		width: 700px;
		// height:200px;
		// background:#fff;
		overflow: hidden;
	}

	.right {
		width: 240px;
		// height:200px;
		// background:#fff;
	}
`;
